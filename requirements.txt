# Runtime dependencies (enough to run + test without system GTK libs).
typer>=0.12
jinja2>=3.1
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
pyyaml>=6.0

# Optional extras (install as needed):
#   pip install -r requirements-pdf.txt   # WeasyPrint PDF output (needs system GTK/Pango)
#   pip install anthropic                 # AI-assisted report drafting
